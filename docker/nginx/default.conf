# server {
#     listen 80;
#     server_name localhost;  # Will be overridden by reverse proxy
#     root /var/www/public;
#     index index.php index.html;

#     error_log  /var/log/nginx/error.log warn;
#     access_log /var/log/nginx/access.log main;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass chutneyads_web_backend:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;

#         # Critical for reverse proxy
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#         fastcgi_param HTTP_X_FORWARDED_FOR $http_x_forwarded_for;
#         fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
#         fastcgi_param HTTP_X_REAL_IP $http_x_real_ip;
#     }

#     location ~ /\.ht {
#         deny all;
#     }

#     client_max_body_size 100M;

#     # Security headers (good practice)
#     add_header X-Frame-Options "SAMEORIGIN";
#     add_header X-Content-Type-Options "nosniff";
#     add_header X-XSS-Protection "1; mode=block";
#     add_header Referrer-Policy "strict-origin-when-cross-origin";
# }


server {
    listen 80;
    index index.php index.html;
    server_name localhost;
    root /var/www/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass backend:9000; # must match service name in docker-compose
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ /\.ht {
        deny all;
    }
}
